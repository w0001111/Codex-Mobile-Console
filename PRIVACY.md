# 发布隐私边界

本版本通过白名单复制必要源码，保留独立副本后再修改，没有复制原部署的私有状态。原运行目录不属于本发布包。

## 已排除

- 原服务器与个人局域网地址、原部署中的私人账号名、机器目录、历史任务 ID。
- 登录密码、密码校验库、API Key、消息平台 Cookie、登录令牌和一次性码。
- SSH 登录私钥与公钥、known_hosts、authorized_keys、证书、ACME 账号及发布签名私钥。
- 真实聊天记录、项目索引数据库、登录记录、发送记录、成果缓存、研究报告、私人图片及真实界面截图。
- 历史部署配置、备份、日志、诊断记录、测试任务上下文、实验候选及旧版本归档。
- Git 历史、依赖目录、Python 环境、下载源码及编译程序。

测试代码只使用合成身份、示例域名和测试数据。代码中的回环地址用于本机监听，不代表任何个人 IP。测试用口令不用于真实账户。

## 部署后产生的数据

程序会在部署者自己的 Mac 上保存身份配置、密码校验值、登录与发送记录、任务索引和任务成果缓存。登录后的页面可能显示任务回复及其明确产出的文件，这是应用的正常用途。清理发布包不能阻止部署者登录后查看自己的数据。

网页 Cookie 设置 Secure、HttpOnly、SameSite；接口保留身份绑定、Host/Origin/CSRF 校验和登录限速。Mac 本机管理员具有恢复权限。消息平台文字入口的访问安全取决于部署者自己的机器人访问限制。

发布扫描会检查允许分发的源码和构建结果，并重新读取压缩包复查。检查结果是针对当前产物的证据，不代表软件不存在未知漏洞。以后加入新文件或复制真实数据，需要重新审查和打包。

## 公开署名与演示截图

作者授权公开的 GitHub 署名和专用发布公钥用于标识发布来源；发布私钥不会进入源码或压缩包。

使用说明中的四张截图由发布版前端在独立浏览器环境中生成。登录输入为空，项目、会话、回复、额度和登录记录全部采用虚构数据。所有 API 请求在浏览器侧由演示数据响应，没有连接真实控制台后端，也没有加载或删改研究会话。

图片经可见内容核查，哈希、尺寸和演示来源声明记录在 `docs/screenshots/manifest.json`。打包工具仅允许这四个明确文件名且哈希一致的 PNG，并拒绝附加元数据块、损坏文件及未登记图片。图片像素隐私依据合成数据隔离和人工查看；文字扫描器不对图片执行 OCR。以后更换截图，必须重新用虚构数据生成、检查可见内容并更新清单。

## 发布者的本机敏感词检查

发布者可把自己的项目简称、主机地址和其他不应公开的值，写入发布目录之外的私有 JSON 数组，再通过 `scripts/package_release.py --deny-file` 传入。检查支持至少四个字符的短名称；敏感词表不会进入发布包，报错只显示文件位置和规则类型。

个人名称和真实数据也不应出现在测试夹具、提交历史、截图或旧发布附件中。更新源码后，应从托管平台重新下载源码及发布附件复查。敏感词比对不能替代人工检查或保证识别所有私人信息。
